import java.*;

public class Error {

	public static void InsertError(String Username, String Provider, StackTraceElement[] el, String Variables)
	{/*

		String Trace="";
		
		for(StackTraceElement e: el)
		{			
			Trace=Trace+e.getLineNumber()+":"+e.getClassName()+"."+e.getMethodName()+"\n";
		}
		
		DBQuery Query=new DBQuery();
		Query.DoQuery("INSERT INTO Error (Username, IDProvider, Stack, Variablen) VALUES("+
		"'"+Username+"',"+
		"'"+Provider+"',"+
		"'"+Trace+"',"+
		"'"+Variables+"')");		*/
	}
	
	
}
